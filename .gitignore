.DS_Store
build/
CMakeCache.txt
CMakeFiles/
cmake_install.cmake
example/*/Makefile

# build dirs — 任意サフィックスの build ディレクトリを全部除外する。
# 以前は build-mg*/ build-mingw*/ しか書いておらず、build-wa/ build-clean/ 等が
# 素通りして生成物が commit されたことがある。
build/
build-*/
*.log

# example build output
example/*/build/
example/*/build-*/
